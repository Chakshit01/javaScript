console.log("HI")
const name="Ram";
let rno = 2;
let isLogged = false;
let ph = null;
let address;


console.log(typeof(address));// will display undefined 
// undefined basicallly defines the uninitialized variable 
console.log(typeof(ph))// will display object
// whereas null depicts that the value is implicitly absent that no value is assigned to the variable

// loosely equal : just checks the value not the datatype
console.log(null == undefined);